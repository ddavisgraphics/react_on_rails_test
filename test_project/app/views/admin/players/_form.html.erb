<%= form_with(model: admin_player, local: true) do |form| %>
  <% if admin_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_player.errors.count, "error") %> prohibited this admin_player from being saved:</h2>

      <ul>
      <% admin_player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :admin_player_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :admin_player_last_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
